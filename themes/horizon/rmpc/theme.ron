#![enable(implicit_some)]
#![enable(unwrap_newtypes)]
#![enable(unwrap_variant_newtypes)]
(
    default_album_art_path: None,
    draw_borders: false,
    show_song_table_header: false,
    symbols: (song: "🎵", dir: "📁", playlist: "🎼", marker: "\u{e0b0}"),
    layout: Split(
        direction: Vertical,
        panes: [
            (
                pane: Pane(Header),
                size: "1",
            ),
            (
                pane: Pane(TabContent),
                size: "100%",
            ),
            (
                pane: Pane(ProgressBar),
                size: "1",
            ),
        ],
    ),
    progress_bar: (
        symbols: ["", "", "⭘", " ", " "],
        track_style: (bg: "#16161c"),
        elapsed_style: (fg: "#e95678", bg: "#16161c"),
        thumb_style: (fg: "#e95678", bg: "#16161c"),
    ),
    scrollbar: (
        symbols: ["│", "█", "▲", "▼"],
        track_style: (),
        ends_style: (),
        thumb_style: (fg: "#e95678"),
    ),
    browser_column_widths: [20, 38, 42],
    text_color: "#d5d8da",
    background_color: "#1c1e26",
    header_background_color: "#16161c",
    modal_background_color: None,
    modal_backdrop: false,
    tab_bar: (active_style: (fg: "#1c1e26", bg: "#e95678", modifiers: "Bold"), inactive_style: ()),
    borders_style: (fg: "#2e303e"),
    highlighted_item_style: (fg: "#e95678", modifiers: "Bold"),
    current_item_style: (fg: "#1c1e26", bg: "#b877db", modifiers: "Bold"),
    highlight_border_style: (fg: "#b877db"),
    song_table_format: [
        (
            prop: (kind: Property(Artist), style: (fg: "#b877db"), default: (kind: Text("Unknown"))),
            width: "50%",
            alignment: Right,
        ),
        (
            prop: (kind: Text("-"), style: (fg: "#e95678"), default: (kind: Text("Unknown"))),
            width: "1",
            alignment: Center,
        ),
        (
            prop: (kind: Property(Title), style: (fg: "#59e1e3"), default: (kind: Text("Unknown"))),
            width: "50%",
        ),
    ],
    header: (
        rows: [
            (
                left: [
                    (kind: Text("["), style: (fg: "#e95678", modifiers: "Bold")),
                    (kind: Property(Status(State)), style: (fg: "#e95678", modifiers: "Bold")),
                    (kind: Text("]"), style: (fg: "#e95678", modifiers: "Bold"))
                ],
                center: [
                    (kind: Property(Song(Artist)), style: (fg: "#fab795", modifiers: "Bold"),
                        default: (kind: Text("Unknown"), style: (fg: "#fab795", modifiers: "Bold"))
                    ),
                    (kind: Text(" - ")),
                    (kind: Property(Song(Title)), style: (fg: "#59e1e3", modifiers: "Bold"),
                        default: (kind: Text("No Song"), style: (fg: "#59e1e3", modifiers: "Bold"))
                    )
                ],
                right: [
                    (kind: Text("Vol: "), style: (fg: "#e95678", modifiers: "Bold")),
                    (kind: Property(Status(Volume)), style: (fg: "#e95678", modifiers: "Bold")),
                    (kind: Text("% "), style: (fg: "#e95678", modifiers: "Bold"))
                ]
            )
        ],
    ),
)
